[package]
name = "dodogo-desktop"
version = "0.4.3"
edition = "2024"
description = "DoDoGo 桌面客户端壳：自动拉起后端服务并以内嵌 WebView 呈现界面"
build = "build.rs"

[dependencies]
tao = "0.30"
wry = "0.50"
ureq = "2"
image = { version = "0.25", default-features = false, features = ["png"] }

[build-dependencies]
winres = "0.1"
